kind: pipeline
name: home
type: docker
steps:
  - name: build
    image: maven:3-jdk-8
    commands:
      - mvn install -Dmaven.test.skip=true
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock
      - /usr/local/java/apache-maven-3.6.1/conf:/root/.m2

